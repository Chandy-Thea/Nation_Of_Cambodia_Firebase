{
  "name": "firebase",
  "version": "1.0.0",
  "description": "The site now reads its province and culture content from **Firestore** instead of hardcoded arrays. Two collections are used:",
  "main": "seed-firestore.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
