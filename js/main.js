/* =========================================================
   Ora Khmer — Site JavaScript
   ========================================================= */

// Currently no site-wide interactive behavior is needed beyond
// Bootstrap's own bundle (navbar toggle, etc). Kept as a hook
// for future page-specific scripts.
